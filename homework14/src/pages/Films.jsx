import { Typography } from "@mui/material";
import React from "react";

const Films = () => {
    return (
        <Typography variant="h4" style={{color: '#dedede'}}>This is Films page</Typography>
    );
};

export default Films;